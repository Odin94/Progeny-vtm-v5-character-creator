procs:
  backend:
    cmd: ["cmd", "/c", "cd backend && pnpm run dev"]
  frontend:
    cmd: ["cmd", "/c", "cd frontend && pnpm start"]
  drizzle-studio:
    cmd: ["cmd", "/c", "cd backend && pnpm run db:studio"]
  
