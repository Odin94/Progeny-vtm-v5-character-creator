@AGENTS.md

## Claude Code Notes
- Keep this file small. Put package-specific guidance in `frontend/AGENTS.md` or `backend/AGENTS.md`, and put deep reference material in `docs/`.
- If a task is mostly confined to `frontend/` or `backend/`, open that directory's `AGENTS.md` before editing.
- Prefer updating the architecture docs when you discover a stable repo invariant that would save future exploration.
